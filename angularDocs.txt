
Run this command to install the dependencies needed to run the app.

npm install

Run this command to build and serve the default app.

ng serve

*filter function in Javascript: const newArray = originalArray.filter(conditionFunc(eachElement))
    -> the function inside the filter function is executed for each element in the original array and it should return
        true to include the element in the new array and not include otherwise